/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // swcMinify: true,

  images: {
    domains: ["random.imagecdn.app"],
  },
};

module.exports = nextConfig;
